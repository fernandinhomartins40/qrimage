# Exemplo de configuração do arquivo .env
# Copie este arquivo para .env e preencha com suas credenciais do Supabase

# Supabase Configuration
VITE_SUPABASE_URL=http://sua-instancia-ip:porta
VITE_SUPABASE_ANON_KEY=sua-anon-key-aqui
VITE_SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# Notas:
# 1. O arquivo .env deve estar na raiz do projeto
# 2. As variáveis com prefixo VITE_ são expostas no frontend
# 3. Nunca commite o arquivo .env no git (já está no .gitignore)